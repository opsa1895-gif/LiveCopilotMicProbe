name: Validate v0.72 confidence latency routing

on:
  push:
    branches: [ v0-72-confidence-latency-routing ]
    paths:
      - '.github/workflows/v0-72-confidence-latency-routing.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@v5
        with:
          ref: v0-72-confidence-latency-routing
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: platform-tools

      - name: Install Android platform
        run: sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.6.1'

      - name: Apply v0.72 source patch
        run: python3 .ci/apply_v072.py

      - name: Run unit tests
        run: gradle --no-daemon :app:testDebugUnitTest

      - name: Build debug APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Remove validation helpers
        run: rm -f .ci/apply_v072.py .github/workflows/v0-72-confidence-latency-routing.yml

      - name: Commit validated patch
        run: |
          git config user.name "opsa1895-gif"
          git config user.email "opsa1895@gmail.com"
          git add app .ci .github/workflows
          git commit -m "Live Copilot v0.72 confidence latency routing"
          git push origin HEAD:v0-72-confidence-latency-routing
